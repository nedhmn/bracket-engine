# @bracket-engine/core

Seeding, single and double elimination, byes, advancing and reopening matches, Swiss pairing, standings. Pure functions over plain data.

```ts
import { generateSeeding, generateSingleElimination } from "@bracket-engine/core";
```
