# tournament-engine

The packages running a tournament on Postgres: participants, phases, matches, drops, top cuts, final standings. Driven by a CLI.

```sh
pnpm --filter tournament-engine cli --help
```
